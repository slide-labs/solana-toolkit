{
  "name": "@slidelabs/solana-sdk",
  "version": "0.0.1",
  "description": "Quickly use the main methods present in Solana, all in one sdk. SPL tokens, transactions, mints.",
  "scripts": {
    "test": "echo \"Error: run tests from root\" && exit 1",
    "clean": "rimraf build",
    "build": "yarn run clean && tsc"
  },
  "dependencies": {},
  "keywords": [
    "solana",
    "blockchain",
    "currency",
    "transaction",
    "nft",
    "mint"
  ],
  "contributors": [
    "dannpl"
  ],
  "main": "build/index.js",
  "module": "build/index.js",
  "files": [
    "build"
  ],
  "publishConfig": {
    "access": "public"
  }
}
